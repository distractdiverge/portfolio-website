import React from 'react';

const FooterComponent : React.FC = () => {
    return (
        <footer className="bg-white p-4">
            <h2 className="p-2">Rainbow Fern</h2>
        </footer>
    );
};

export default FooterComponent;