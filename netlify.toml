[build]
  command = "npm run build"
  publish = "dist"

# The redirect rule for SPA is already handled by public/_redirects
# If you prefer to manage it here, you could add:
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
